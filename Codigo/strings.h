#ifndef _STRINGS_H_   // Nao incluir duas vezes
#define _STRINGS_H_

typedef struct{
	int tamanho_lexema;
	int fator_limite;
	char * lexema;
} tString;

//typedefs
//iniciar_string();
//realocar_string();
//copiar_string();

//prototipes

#endif
